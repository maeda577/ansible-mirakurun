- name: Set authorized key
  authorized_key:
    user: "{{ ansible_user }}"
    key: "{{ lookup('file', '/home/' + lookup('env', 'USER') + '/.ssh/id_rsa.pub') }}"

- name: Set hostname
  hostname:
    name: "{{ inventory_hostname }}"
    use: systemd
  become: yes

- name: Set timezone
  timezone:
    name: Asia/Tokyo
  become: yes
